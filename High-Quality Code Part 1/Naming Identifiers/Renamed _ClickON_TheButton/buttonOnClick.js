function buttonOnClick(event, arguments) {
    var browser = window.navigator.appCodeName,
        isCurrentBrowserMozzila = browse == "Mozilla";

    if (isCurrentBrowserMozzila) {
        alert("Yes");
    } else {
        alert("No");
    }
}